package main

/*
Use Go log module
func LogWrite(format string, a ...any) {

	now := time.Now().UTC()
	millis := time.Now().UnixMilli() % 1e3

	s1 := fmt.Sprintf("[%s.%-03d] %s", now.Format("2006-01-02 15:04:05"), millis, format)
	fmt.Printf(s1, a...)
}
*/
